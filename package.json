{
  "private": true,
  "workspaces": [
    "lib",
    "docs",
    "example"
  ],
  "scripts": {
    "build": "yarn build:lib && yarn build:docs",
    "build:lib": "yarn workspace primer-react-prototype build",
    "build:docs": "yarn workspace docs build"
  }
}
